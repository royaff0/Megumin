<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <attr name="contentBackground" format="color"/>
    <attr name="textColorPrimary" format="color"/>
    <attr name="textColorSecondary" format="color"/>
    <attr name="textColorLowLight" format="color"/>
    <attr name="backgroundFore" format="color"/>
    <attr name="detailContentBackground" format="color"/>
    <attr name="detailContentImageForeground" format="color"/>
    <attr name="detailBackground" format="color"/>
    <attr name="textColorTitle2" format="color"/>

</resources>
